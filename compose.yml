services:
  postgres:
    image: postgres:16
    container_name: lite
    restart: always
    environment:
      POSTGRES_DB: lite
      POSTGRES_USER: ****
      POSTGRES_PASSWORD: ****
    ports:
      - "5432:5432"
